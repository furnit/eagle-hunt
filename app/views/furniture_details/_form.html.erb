<%= form_for(furniture_detail) do |f| %>
  <% if furniture_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(furniture_detail.errors.count, "error") %> prohibited this furniture_detail from being saved:</h2>

      <ul>
      <% furniture_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :size_parche %>
    <%= f.text_field :size_parche %>
  </div>

  <div class="field">
    <%= f.label :size_kanaf %>
    <%= f.text_field :size_kanaf %>
  </div>

  <div class="field">
    <%= f.label :size_abr %>
    <%= f.text_field :size_abr %>
  </div>

  <div class="field">
    <%= f.label :available %>
    <%= f.check_box :available %>
  </div>

  <div class="field">
    <%= f.label :images %>
    <%= f.text_field :images %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
