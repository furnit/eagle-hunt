$("#filterrific_results").html("<%= j render "#{params[:controller]}/list" %>");

<% if @filterrific.instance_variable_defined?(:@search_query) and not @filterrific.search_query.to_s.empty? %>
$("#filterrific_results table > tbody > tr > td").highlight("<%= j @filterrific.search_query.to_s %>")
<% end %>