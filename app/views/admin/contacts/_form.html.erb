<% form_id = '' %>
<%= remote_form(admin_contact) do |f| -%>
  <% form_id = f.options[:html][:id] %>
  <div  class='block'>
      <div class="field">
        <%= f.text_field :name %>
      </div>

      <div class="field">
        <%= f.text_area :phone_numbers %>
      </div>

      <div class="field">
        <%= f.text_area :address %>
      </div>

      <div class="field">
        <%= f.text_area :comment %>
      </div>

      <div class='clearfix'></div>
  </div>

  <div class="actions field pull-left">
    <%= f.submit submit_text,  data: { disable_with: "درحال ثبت اطلاعات..." }, class: 'btn btn-primary' %>
  </div>
  <div class='clearfix'></div>
<% end %>