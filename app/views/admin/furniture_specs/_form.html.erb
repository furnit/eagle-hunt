<%= form_for(admin_furniture_spec) do |f| %>
  <% if admin_furniture_spec.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_furniture_spec.errors.count, "error") %> prohibited this admin_furniture_spec from being saved:</h2>

      <ul>
      <% admin_furniture_spec.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
