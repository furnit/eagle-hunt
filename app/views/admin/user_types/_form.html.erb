<div class='col-md-offset-2 col-md-8' style='padding: 30px;'>
  <%= redirect_form(admin_user_type) do |f| %>
    <% if admin_user_type.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(admin_user_type.errors.count, "error") %> prohibited this admin_user_type from being saved:</h2>
  
        <ul>
        <% admin_user_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    
    <div class='block'>
      <div class="field">
        <%= f.text_field :name, label: 'نام نوع کاربری' %>
      </div>
    
      <div class="field">
        <%= f.text_area :comment, label: 'توضیحات' %>
      </div>
    
      <div class="field">
        <%= f.number_field :auth_level, label: 'سطح دسترسی' %>
      </div>
    </div>
    
    <div class="actions pull-left">
      <%= f.submit 'ثبت اطلاعات' %>
    </div>
  <% end %>
</div>
