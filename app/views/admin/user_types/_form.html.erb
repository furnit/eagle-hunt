<%= form_for(admin_user_type) do |f| %>
  <% if admin_user_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_user_type.errors.count, "error") %> prohibited this admin_user_type from being saved:</h2>

      <ul>
      <% admin_user_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>

  <div class="field">
    <%= f.label :auth_level %>
    <%= f.number_field :auth_level %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
