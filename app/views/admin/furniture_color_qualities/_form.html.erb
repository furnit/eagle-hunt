<%= form_for(admin_furniture_color_quality) do |f| %>
  <% if admin_furniture_color_quality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_furniture_color_quality.errors.count, "error") %> prohibited this admin_furniture_color_quality from being saved:</h2>

      <ul>
      <% admin_furniture_color_quality.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
