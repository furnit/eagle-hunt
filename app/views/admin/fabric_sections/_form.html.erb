<%= form_for(admin_fabric_section) do |f| %>
  <% if admin_fabric_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_fabric_section.errors.count, "error") %> prohibited this admin_fabric_section from being saved:</h2>

      <ul>
      <% admin_fabric_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <div class="field">
    <%= f.label :text %>
    <%= f.text_field :text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
