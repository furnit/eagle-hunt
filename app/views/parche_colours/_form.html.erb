<%= form_for(parche_colour) do |f| %>
  <% if parche_colour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parche_colour.errors.count, "error") %> prohibited this parche_colour from being saved:</h2>

      <ul>
      <% parche_colour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :details %>
    <%= f.text_field :details %>
  </div>

  <div class="field">
    <%= f.label :cost %>
    <%= f.text_field :cost %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
