<%= render 'devise/registrations/header' %>

<legend><h2><%= t('.forgot_your_password') %></h2></legend>

<p>جهت اینکه تنظیم مجدد رمز عبور خود، شماره موبایل خود را در زیر وارد کنید.</p>

<%= redirect_form(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <div class="field">
    <%= f.telephone_field :phone_number, placeholder: '09*********', autofocus: true, maxlength: 11, required: true %>
  </div>
  
  <div class="field">
	  <%= recaptcha_tag has_error: flash[:recaptcha_error] %>
	</div>
	
  <div class="actions field">
    <%= f.submit 'ارسال کد موقت' %>
  </div>
<% end %>

<div class="field">
<%= render "devise/shared/links" %>
</div>
<%= render 'devise/registrations/footer' %>
