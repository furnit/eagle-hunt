<%= form_for(page_item) do |f| %>
  <% if page_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_item.errors.count, "error") %> prohibited this page_item from being saved:</h2>

      <ul>
      <% page_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :controller %>
    <%= f.text_field :controller %>
  </div>

  <div class="field">
    <%= f.label :action %>
    <%= f.text_field :action %>
  </div>

  <div class="field">
    <%= f.label :details %>
    <%= f.text_field :details %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
